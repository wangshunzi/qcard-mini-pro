<view
  class="nav {{overlay ? 'nav-overlay' : ''}} {{light ? 'nav-light' : ''}} {{navScrolled ? 'nav-scrolled' : ''}} {{border ? 'nav-border' : ''}}"
  style="height:{{totalHeight}}px;box-shadow:{{navShadow}};color:{{navForeground}};--nav-background:{{background}};"
>
  <view class="nav-surface" style="opacity:{{navSurfaceOpacity}};backdrop-filter:blur({{navBlur}}rpx);"></view>
  <view
    class="nav-row"
    style="height:{{controlRowHeight}}px;margin-top:{{controlRowTop}}px;--immersive-control-height:{{controlRowHeight}}px;"
  >
    <view class="nav-controls" style="max-width:{{controlsMaxWidth}}px;">
      <button wx:if="{{back}}" class="nav-back" bindtap="goBack" aria-label="返回" hover-class="nav-control-hover">
        <view class="nav-back-icon">
          <ui-icon name="chevron-left" size="{{44}}" color="{{navForeground}}" />
        </view>
      </button>
      <view wx:else class="nav-leading">
        <slot name="leading"></slot>
      </view>
      <view class="nav-action">
        <slot name="action"></slot>
      </view>
    </view>
    <view class="nav-center" style="padding-right:{{capsuleReservedWidth}}px;opacity:{{navTitleOpacity}};">
      <text wx:if="{{title}}" class="nav-title">{{title}}</text>
      <slot name="center"></slot>
    </view>
  </view>
</view>
<view wx:if="{{!overlay}}" style="height:{{totalHeight}}px;"></view>
