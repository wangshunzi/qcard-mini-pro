# H5 卡片交互对齐审计

日期：2026-07-27  
范围：除本轮前序已完成的拼图卡外，其余 9 类卡片的完整模式交互状态。

## 结论

当前 9 类卡片均能进入预期关键状态，没有发现阻断学习流程的残留问题。截图全部来自本轮修改后的微信开发者工具自动化运行，生产卡片使用线上真实数据；选择、连线、分类使用与共享 Schema 一致的审计 fixture。

## 状态检查

1. 认知卡：健康。中文默认语言、顺口溜播放态、媒体互斥和离屏重置正常。
   - `01-recognition-playing.png`
2. 识字卡：健康。对话弹层、头像、音频入口、笔画/临摹状态重置正常。
   - `02-literacy-conversation.png`
3. 故事卡：健康。段落点击跳播、角色头像、活跃段落、进度和倍速正常。
   - `03-story-active-dialogue.png`
4. 听音识物卡：健康。提示胶囊、播放按钮、进度环、快速刮擦采样和科普切换正常。
   - `04-sound-object-scratching.png`
5. 选择卡：健康。错误选择锁定、红色反馈和 760ms 复位语义与 H5 一致。
   - `05-choice-wrong.png`
6. 轮廓识别卡：健康。错误态锁定、反馈复位、正确揭示和科普阶段正常。
   - `06-silhouette-wrong.png`
7. 听力卡：健康。材料续播、答题切换、完成锁定、文稿默认状态和媒体互斥正常。
   - `07-listening-answering.png`
8. 连线卡：健康。点击连接、拖拽连接、单端替换、错误检查、完成锁定和坐标兼容正常。
   - `08-matching-connected.png`
9. 分类卡：健康。点击分配、拖拽分配、拖回物品池、错误状态清理和规则推进正常。
   - `09-classification-assigned.png`

## 本轮修复

- 识字卡切换多音字时同步重置动画、临摹、错误次数、完成态、对话和媒体。
- 听音识物卡快速移动时沿完整手势线段采样，避免真机快速刮擦漏算。
- 听音识物卡改为双 Canvas：底层刮擦，顶层提示、播放按钮和进度环；触摸按按钮命中区和刮擦区分流。
- 轮廓识别卡错误反馈期间锁定重复提交。
- 听力卡暂停后从原位置续播；切题只停止答题反馈，不错误重置材料；完成后禁止切题；重置恢复默认文稿展开态。
- 连线卡、分类卡兼容 `client/page/local` 三类真机触摸坐标。
- 分类卡把物品拖回池后清除旧的红色错误态和已检查状态。
- 认知卡优先选择与 H5 相同的 `zh` 语言。
- 故事卡只读模式禁用进度条拖动。

## 验证

- `pnpm check`：通过；8 个测试文件、86 条测试全部通过。
- `pnpm release:check`：通过；首页、汉字动态数据、协议文档、55 个 Client 卡片素材均正常。
- 微信开发者工具正式 preview：通过；总包 754.5 KB，主包 537.5 KB，卡片分包 158.9 KB，设置分包 58.1 KB。
